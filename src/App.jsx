import AuthorizeInputs from './authorize/Authorize-inputs/AuthorizeInputs';

export default function App () {
  return <main>
    <article>
      <h1>Authorize</h1>
      <AuthorizeInputs />
    </article>
  </main>;
}